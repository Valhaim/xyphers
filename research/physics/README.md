# Valhaim kernel archive

The [kernel record](valhaim-thermodynamic-kernel/README.md) contains the frozen protocol, sealed runner, exact results, and exclusions copied unchanged from THAIM revision `c46f4a808`. `SOURCE-SHA256SUMS` records all imported files.

These are historical execution records. The seal binds the original Git history, binary, toolchain, and paths; a fresh clone of this publication does not recreate that sealed execution environment. Inspect the artifacts directly. A new execution would need a separately identified reproduction wrapper and receipt; do not rewrite the original seal or outputs.

For an immediately runnable independent thermodynamic reproduction, use the [sixteen-state study](../code/xypher-thermodynamics-proof/README.md).
